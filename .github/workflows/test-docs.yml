name: Test Docs
on:
  pull_request:
    paths:
      - "docs/**"

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Validate CSS
        # Note: Script only uses Python standard library, no additional deps needed
        run: python3 script/validate-css.py
      - uses: oven-sh/setup-bun@v1
      - name: Install
        working-directory: docs
        run: bun install
      - name: Build
        working-directory: docs
        run: |
          bun run build
